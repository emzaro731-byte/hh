import React,{useState}from'react';
import{ActivityIndicator,Image,Linking,Modal,Pressable,ScrollView,StyleSheet,Text,TextInput,View}from'react-native';
import{generateAI,waitForAI,AIGeneration}from'../lib/ai';

type Props={visible:boolean;onClose:()=>void;dark?:boolean};
export default function AIStudio({visible,onClose,dark=false}:Props){
 const[mode,setMode]=useState<'chat'|'image'|'video'|'music'>('chat');const[prompt,setPrompt]=useState('');const[loading,setLoading]=useState(false);const[result,setResult]=useState<AIGeneration|null>(null);const[model,setModel]=useState('flux-2/flex-text-to-image');const[status,setStatus]=useState('');
 const bg=dark?'#080b12':'#f6f8fc',card=dark?'#121824':'#fff',fg=dark?'#f8fafc':'#111827',muted=dark?'#94a3b8':'#667085';
 async function run(){if(!prompt.trim()||loading)return;setLoading(true);setResult(null);setStatus('Starting generation…');try{const started=await generateAI(mode,prompt.trim(),{model,aspectRatio:mode==='video'?'9:16':'1:1',duration:5,quality:'720p'});if(mode==='chat'){setResult(started);return}if(!started.taskId)throw new Error('No generation task was returned');setStatus('Generating…');const done=await waitForAI(mode,started.taskId,setStatusFromResult);setResult(done);setStatus('Complete');}catch(e){setResult({message:e instanceof Error?e.message:'Generation failed'});setStatus('Failed')}finally{setLoading(false)}}
 function setStatusFromResult(x:AIGeneration){if(x.status)setStatus('Generating… '+x.status)}
 const choose=(x:'chat'|'image'|'video'|'music')=>{setMode(x);setResult(null);setStatus('');setModel(x==='image'?'flux-2/flex-text-to-image':x==='video'?'runway':x==='music'?'suno':'')};
 const openResult=(url:string)=>Linking.openURL(url).catch(()=>{});
 return <Modal visible={visible} animationType="slide" onRequestClose={onClose}><View style={[s.root,{backgroundColor:bg}]}><View style={s.header}><Pressable onPress={onClose}><Text style={[s.close,{color:fg}]}>×</Text></Pressable><View style={{flex:1}}><Text style={[s.title,{color:fg}]}>GG AI Studio</Text><Text style={{color:muted}}>Chat • Images • Video • Music</Text></View><Text style={s.spark}>✦</Text></View><ScrollView contentContainerStyle={s.content}><View style={s.modes}>{(['chat','image','video','music'] as const).map(x=><Pressable key={x} onPress={()=>choose(x)} style={[s.mode,{backgroundColor:mode===x?'#2563eb':card}]}><Text style={{fontSize:20}}>{x==='chat'?'🤖':x==='image'?'🖼️':x==='video'?'🎬':'🎵'}</Text><Text style={{color:mode===x?'#fff':fg,fontWeight:'800',marginTop:5}}>{x[0].toUpperCase()+x.slice(1)}</Text></Pressable>)}</View><Text style={[s.label,{color:fg}]}>{mode==='chat'?'Ask GG AI anything':mode==='image'?'Describe the image':'Describe your '+mode}</Text><TextInput multiline value={prompt} onChangeText={setPrompt} placeholder={mode==='image'?'A cinematic Nigerian music artist on a futuristic rooftop at night…':mode==='video'?'A cinematic music video in Lagos at night, neon lights, smooth camera movement…':mode==='music'?'High-energy Nigerian Afrobeats with Afro-fusion drums, catchy hook and premium production…':'Write a chorus for my next song…'} placeholderTextColor={muted} style={[s.prompt,{backgroundColor:card,color:fg}]}/>{mode!=='chat'&&<><Text style={[s.label,{color:fg}]}>Model</Text><View style={[s.chip,{backgroundColor:card}]}><Text style={{color:fg}}>{model}</Text></View></>}<Pressable disabled={loading||!prompt.trim()} onPress={run} style={[s.generate,{opacity:(loading||!prompt.trim())?.5:1}]}><Text style={s.generateText}>{loading?'Creating…':mode==='chat'?'Ask GG AI':'Generate '+mode}</Text></Pressable>{loading&&<View style={s.progress}><ActivityIndicator size="small"/><Text style={{color:muted,marginLeft:10}}>{status||'Working…'}</Text></View>}{result?.message&&<View style={s.error}><Text style={{color:'#ef4444'}}>{result.message}</Text></View>}{result?.url&&<View style={[s.result,{backgroundColor:card}]}>{mode==='image'?<Image source={{uri:result.url}} style={s.resultImage}/>:<Pressable onPress={()=>openResult(result.url!)} style={s.open}><Text style={s.openText}>{mode==='video'?'▶ Open video':'♫ Open audio'}</Text></Pressable>}</View>}{result?.urls?.map((u,i)=><View key={i} style={[s.result,{backgroundColor:card}]}>{mode==='image'?<Image source={{uri:u}} style={s.resultImage}/>:<Pressable onPress={()=>openResult(u)} style={s.open}><Text style={s.openText}>{mode==='video'?'▶ Open video':'♫ Open audio'} {result.urls!.length>1?i+1:''}</Text></Pressable>}</View>)}{result?.message&&mode==='chat'&&<View style={[s.chatResult,{backgroundColor:card}]}><Text style={{color:fg,fontSize:16,lineHeight:24}}>{result.message}</Text></View>}{result?.taskId&&<View style={[s.task,{backgroundColor:card}]}><Text style={{color:fg,fontWeight:'800'}}>{status==='Complete'?'Generation complete':'Generation task'}</Text><Text style={{color:muted,marginTop:4}}>{result.taskId}</Text></View>}<View style={[s.tip,{backgroundColor:card}]}><Text style={{fontSize:18}}>⚡</Text><Text style={{flex:1,color:muted}}>AI keys stay on the server. GG AI uses your Supabase Edge Function so KIE/OpenAI secrets are never placed in the APK.</Text></View></ScrollView></View></Modal>}
const s=StyleSheet.create({root:{flex:1},header:{height:78,padding:14,flexDirection:'row',alignItems:'center',gap:12},close:{fontSize:38},title:{fontSize:22,fontWeight:'900'},spark:{fontSize:25,color:'#2563eb'},content:{padding:16,paddingBottom:40},modes:{flexDirection:'row',gap:8,marginBottom:22},mode:{flex:1,minHeight:82,borderRadius:18,alignItems:'center',justifyContent:'center',padding:8},label:{fontWeight:'800',fontSize:15,marginBottom:8},prompt:{minHeight:150,borderRadius:18,padding:15,textAlignVertical:'top',fontSize:15,marginBottom:18},chip:{padding:14,borderRadius:14,marginBottom:18},generate:{backgroundColor:'#2563eb',borderRadius:17,paddingVertical:15,alignItems:'center'},generateText:{color:'#fff',fontWeight:'900',fontSize:16},progress:{marginTop:18,padding:14,borderRadius:14,backgroundColor:'#e8eefc',flexDirection:'row',alignItems:'center'},error:{marginTop:16,padding:14,borderRadius:14,backgroundColor:'#fee2e2'},result:{marginTop:18,padding:12,borderRadius:18},resultImage:{width:'100%',height:330,borderRadius:14},open:{paddingVertical:16,alignItems:'center'},openText:{color:'#2563eb',fontSize:17,fontWeight:'900'},chatResult:{marginTop:18,padding:16,borderRadius:18},task:{marginTop:18,padding:16,borderRadius:18},tip:{marginTop:18,padding:15,borderRadius:18,flexDirection:'row',gap:10}});